---
title: Sample 11ty
post: collections.post {# getting the post tagged in the .md files #}
---
<!doctype html>
<html lang="en">
 <head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/css/main.css">
 </head>
 <body>
 	<h1>{{ title }}</h1>
  <ul>
    {%- for post in collections.post -%}
      <li>{{ post.data.title }}</li>
    {%- endfor -%}
  </ul>
  <div class="container">
    {%- for animal in animals -%}
      <div class="animal">
        <span>
          Name: {{ animal.name }}
        </span>
        <span>
          Country: {{ animal.country }}
        </span>
        <span>
          Description: {{ animal.description }}
        </span>
      </div>
    {%- endfor -%}
  </div>
 </body>
</html>
